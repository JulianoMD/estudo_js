// Math =  Objeto intrínseco que fornece funcionalidade matemática básica e constantes

let x = 3.1;
let y = 5;
let z = 9;
let maximum;
let minimum;

// x = Math.trunc(x); //Remove todos os decimais do valor
// x = Math.round(x); // Arredonda numero decimal para o inteiro mais proximo
// x = Math.floor(x); // Arredonda para baixo um numero float para inteiro
// x = Math.ceil(x); //  Arredonda para cima um numero float para inteiro 
// x = Math.pow(x, 2); // x² === x * x
// x = Math.sqrt(x); // √x 
// x = Math.abs(x); // Retorna sempre o mesmo numero só que positivo
// maximum = Math.max(x, y, z); // Retorna o valor maximo
// minimum = Math.min(x, y, z); // Retorna o valor minimo
// x = Math.PI; // retorna o valor de Pi

console.log(x)